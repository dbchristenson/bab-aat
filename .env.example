# Copy this file to .env and fill in your Modal credentials.
# All other settings have sensible defaults for local development
# (SQLite database, local filesystem storage, Redis on localhost:6379).

# Required: Modal credentials for OCR inference (https://modal.com)
MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
